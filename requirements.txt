numpy>=2.0.0,<2.3.0
scipy>=1.14.0
opencv-python>=4.5.0
psutil>=5.8.0
requests>=2.25.0
rapidfuzz>=2.0.0
websocket-client>=1.0.0
mss>=6.1.0
Pillow>=8.0.0
pystray>=0.19.0
PyQt6>=6.5.0
# EasyOCR for GPU-accelerated OCR (requires scipy for ndimage)
easyocr>=1.7.0

# PyTorch with CUDA support (GPU acceleration)
# For NVIDIA GPU users: Install with CUDA support using:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# For CPU-only (no GPU): Install with:
#   pip install torch torchvision
# Note: The app will auto-detect your GPU and provide installation instructions if needed
torch>=2.0.0
torchvision>=0.15.0